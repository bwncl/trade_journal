jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # ← DEBUG STEP START
      - name: Debug Backend FS
        run: |
          echo "=== Backend working dir ==="
          pwd
          echo "=== Files: ==="
          ls -R .
      # ← DEBUG STEP END

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      …  

  frontend:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v3

      # ← DEBUG STEP START
      - name: Debug Frontend FS
        run: |
          echo "=== Frontend working dir ==="
          pwd
          echo "=== Files: ==="
          ls -R .
      # ← DEBUG STEP END

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      …

